import { createIcons, Github, Linkedin, Menu, Twitter, X } from 'lucide';
import { handleHeaderNavMenu } from "./header";

createIcons({ icons: { Menu, X, Github, Linkedin, Twitter }});

handleHeaderNavMenu()